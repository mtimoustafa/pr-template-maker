# Magic comment to freeze string literals
# frozen_string_literal: true

# Gem sources
source "https://rubygems.org"

# Comment required by RVM to specify preferred ruby version
#ruby=3.0.1
ruby ">= 3.0.1"

# Workaround for Bundler bug:
# https://stackoverflow.com/questions/41454333/meaning-of-new-block-git-sourcegithub-in-gemfile
git_source(:github) {|repo_name| "https://github.com/#{repo_name}" }

# Gems
gem 'erb'
gem 'pastel'
gem 'tty-command'
gem 'tty-prompt'
